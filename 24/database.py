#The sqlite3 library in Python provides a simple and efficient way to interact with SQLite databases.
import sqlite3

#conect our file/app with database (or create db if it doesnt exist)
conection = sqlite3.connect('mydatabase.db');
